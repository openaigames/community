# Existing published games

`legacy.json` preserves dodo, which was already manually published before automatic catalog submission was introduced. It does not change the status of community PR #5. Add new games with `demos/<id>/game.json` instead.
